/**
 * Whether the graph.json is encrypted with a password.
 *
 * Set at build time based on whether the selected config has a non-empty password.
 */
export const ENCRYPTED: boolean = __EC_ENCRYPTED__;
